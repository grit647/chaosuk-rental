// Top-level build file — เพิ่ม plugin ที่ต้องใช้ทั้งโปรเจ็กต์ตรงนี้
// (2026-07-23) สร้างเป็น "เปลือกแอป" ห่อเว็บเช่าสุขที่มีอยู่แล้ว
// (chaosuk-rental.onrender.com) — ไม่มีโค้ดธุรกิจ/ฐานข้อมูลอะไรใหม่เลย
// จอเดียวคือ WebView ที่เปิดหน้า /login ของเว็บจริงตรงๆ (สำเนาโครงสร้าง
// เดียวกับ check-service-24/android-app/ แต่ตัดฟีเจอร์เสียงพูด/Foreground
// Service ออกทั้งหมด เพราะเช่าสุขไม่มีระบบเลขาเสียง — ต้องการแค่ทางลัด
// เปิดแอปเข้าเว็บตรงๆ เท่านั้น)
plugins {
    id 'com.android.application' version '8.5.2' apply false
}
