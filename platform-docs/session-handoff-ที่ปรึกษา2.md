# Handoff สำหรับ "ที่ปรึกษา 2" — สรุปสถานะข้ามแพลตฟอร์ม (2026-08-13)

> เขียนไว้เพราะ session เดิม ("ที่ปรึกษางาน") ยาวเกิน 890K token แล้ว
> แพงเกินคุ้มต่อการคุยต่อ — เปิด session ใหม่นี้แทน อ่านไฟล์นี้ก่อนเริ่ม
> งานเพื่อรู้บริบทว่าค้างอะไรอยู่บ้าง รายละเอียดเชิงลึกของแต่ละแพลตฟอร์ม
> อยู่ใน CLAUDE.md/platform-docs ของโปรเจกต์นั้นๆ เองอยู่แล้ว ไฟล์นี้แค่
> สรุป "ที่ต้องรู้ก่อนเริ่ม" ข้ามแพลตฟอร์ม

## โปรเจกต์ที่คุณต้นดูแล (5 ตัว)

1. **เช่าสุข** (chaosuk-rental) — `D:\homeob1` — ระบบจัดการหอพัก/อพาร์ตเมนต์
2. **เช็คเซอร์วิส** (check-service-24) — `D:\check-service-24` — ระบบจองคิว/จัดการร้านบริการ
3. **ร้านค้าส่ง** (wholesale-order) — `D:\wholesale-order` — ระบบจัดการร้านขายส่ง-ปลีก
4. **ช.นายท้าย** (chor-naithai) — `D:\chor-naithai` — แพลตฟอร์มที่ 4 ดูแล/การตลาดให้ 3 ตัวบน (มีเลขา="น้อง6" + ตี๋น้อย/อาตี๋/ตี๋เล็ก)
5. **ชัวร์ทรัพย์** (chuarsup) — โปรเจกต์แยกต่างหาก ไม่ได้แตะในเซสชันนี้

## ค้างอยู่ตอนนี้ — ยังไม่จบ

### 🔴 ด่วน: Tuya IoT ของเช่าสุขหลุดการเชื่อมต่อ
**สาเหตุ:** "Cloud Develop Base Resource Trial" บน Tuya IoT Platform หมด
โควตา (0.2/0.2 USD, สถานะ Suspended) — ทำให้ `/api/tuya/health` ตอบ
`"IoT Core trial quota is exhausted"` เชื่อมต่อ Tuya ไม่ได้เลยทั้งน้ำ/ไฟ
**สถานะล่าสุด:** คุณต้นกำลังกรอกฟอร์มขอ "Extend Trial Period" (6 เดือน,
Independent Software Vendor) ที่ platform.tuya.com — **ยังไม่ submit
เสร็จ/รอ Tuya อนุมัติ** — ระหว่างรอ Tuya พิจารณา ระบบน้ำ/ไฟจริงจะยังใช้
งานไม่ได้ ถ้าเร่งด่วนอาจต้องพิจารณาจ่ายเงินแพ็ก Flagship/Corporate แทน

### 🟡 check-service-24 — LINE ไม่ตอบสนอง (สงสัยว่าเกิดจาก Render sleep)
คุณต้นแจ้งกดปุ่ม "คิวทั้งหมด" ใน LINE OA หลายรอบ ขึ้น "อ่านแล้ว" แต่ไม่มี
คำตอบกลับมาเลย — **ยังไม่ได้เช็คว่า Webhook redelivery เปิดอยู่ไหม**
(เช็คแค่ wholesale-order ไปแล้ว พบว่าเปิดอยู่แล้ว) — ต้องเข้า LINE
Developers Console ของ check-service-24 เช็ค/เปิด Webhook redelivery
เป็นขั้นแรก (ดูวิธีเต็มใน chaosuk-rental's CLAUDE.md หัวข้อ "Render
free-tier sleep breaks LINE messages")

## สรุปสิ่งที่เสร็จแล้วในเซสชันก่อนหน้า (รายละเอียดเต็มอยู่ในแต่ละโปรเจกต์เอง)

- **เช่าสุข:** แก้สูตรคำนวณค่าน้ำสำหรับห้องยังไม่คาลิเบรต, แก้จอแสดงผล
  ไม่ตรงกับตัวคำนวณบิลจริง (น้ำ+ไฟ), ซ่อนช่อง SET NEW, เพิ่มแจ้งไลน์ผล
  คาลิเบรต — ดูรายละเอียดเต็มใน `D:\homeob1\CLAUDE.md` หัวข้อ "Water
  billing formula fixed for uncalibrated rooms (2026-08-12)"
- **ช.นายท้าย:** ระบบเรียนรู้อัลกอริทึม, เชื่อมต่อ TikTok, คลังรูปภาพ/
  วิดีโอ, แก้ AI tool-calling ให้เรียกได้หลายรอบ, popup เลือกช่องทาง
  โพสต์, ห้องแชทอัปเดทสด, แก้บั๊ก prefix พอกพูน, เพิ่ม tool ให้เลขา
  (read_room_history, get_marketing_posts_status, get_facebook_page_
  insights), บันทึกนิสัยพูดธรรมชาติ+กรองเสียงรบกวนโหมดเรียลไทม์ให้เลขา,
  popup "กำลังทำงานอยู่" — ดูรายละเอียดเต็มใน `D:\chor-naithai\
  platform-docs\`
- **ร้านค้าส่ง:** เปลี่ยน credentials อ่านรูปสินค้า (Gemini Vision) เป็น
  ชุดเดียวกับตี๋น้อย (ยืมจาก chaosuk-rental) หลังเจอ error billing —
  ทดสอบแล้วใช้งานได้จริง

## บทเรียนสำคัญที่ควรรู้ก่อนเริ่มงานใหม่

1. **Session ยาวเกินไปจะแพงเครดิตขึ้นเรื่อยๆ แบบทวีคูณ** (ทุก token
   context ต้องอ่านซ้ำทุกครั้งที่ตอบ) — ถ้า session ไหนยาวเกิน ~500K
   token ควรเริ่มใหม่แทนคุยต่อ
2. **หลายโปรเจกต์แยก Google Sheet ต่อตึก/ต่อร้าน** (multi-tenant) —
   คอลัมน์/แท็บใหม่ต้อง migrate ทุกตึกแยกกัน ไม่ใช่แค่ตึกหลัก
3. **Render free tier หลับเมื่อไม่มีคนใช้งาน** — ทำให้ LINE webhook พลาด
   ได้ถ้าไม่เปิด Webhook redelivery ในทุกช่องทาง LINE ของทุกโปรเจกต์
4. **คุณต้นชอบให้เสนอตัวเลือกแล้วให้เลือกเอง** ไม่ใช่ตัดสินใจให้เลย
   (ยกเว้นบอก "ว่าไปเลยครับ"/"ตัดสินใจให้เลยครับ")
