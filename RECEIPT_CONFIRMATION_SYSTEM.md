# ระบบยืนยันการรับใบเสร็จ (Receipt Delivery Confirmation) — 2 ส.ค. 2569

> **สถานะ: สร้างเสร็จแล้ว ทดสอบ syntax ผ่านหมด แต่ยังไม่มีผลจริงกับ
> ตึกไหนเลย** — ต้องกด "🆕 อัปเดต" ที่หน้า "ทุกตึกในระบบ (Server only)"
> ให้ตึกนั้นก่อน (platformVersion >= 6) ถึงจะเริ่มทำงานจริง — ดูหัวข้อ
> "Staged-rollout gate" ด้านล่าง

## ที่มา

บั๊กจริงที่เจอ: ปุ่ม **"ส่งข้อมูล (LINE)"** ในตาราง "ใบแจ้งหนี้ที่รอชำระ"
เปลี่ยนสี/ข้อความเป็น "✓ ส่งไลน์แล้ว" หลังส่งครั้งแรก แต่**ยังกดซ้ำได้
จริง** — กดซ้ำแล้ว `sendReceiptLine` สร้างใบเสร็จใหม่จากข้อมูล**ปัจจุบัน**
อีกครั้ง ซึ่งอาจไม่ตรงกับใบที่ผู้เช่าได้รับไปแล้วครั้งแรก (เช่น ถ้าแก้ไข
ข้อมูลบิลระหว่างนั้น)

คุณต้นขอให้แก้ + เพิ่มระบบยืนยันเต็มรูปแบบทั้ง 2 ฝั่ง (ผู้เช่า + เจ้าของ)

## สถาปัตยกรรม

### ฝั่งผู้เช่า — ยืนยันว่าได้รับใบแจ้งหนี้จริง

```
ส่งใบเสร็จ (มีปุ่ม "✅ ยืนยันได้รับแล้ว" แนบมาด้วยเสมอ)
        │
        ▼
สถานะ "✓ ส่งไลน์แล้ว (รอยืนยัน)" — ปุ่ม "ส่งข้อมูล" ปิดใช้งานถาวร
        │
        ├── ผู้เช่ากดปุ่มยืนยันใน LINE ──► "✓ ยืนยันแล้ว" (จบ)
        │
        └── ผ่านไป 24 ชม. ไม่กด ──► scheduler ส่งซ้ำอัตโนมัติ (ครั้งที่ 2)
                                            │
                                            ├── กดยืนยัน ──► "✓ ยืนยันแล้ว" (จบ)
                                            │
                                            └── ผ่านไปอีก 24 ชม. ไม่กด
                                                    ──► แจ้งเจ้าของทาง LINE ให้ติดต่อเอง
```

**จุดสำคัญ:** การส่งซ้ำ (retry) ใช้ `receiptImageUrl` ที่บันทึกไว้จาก
การส่งครั้งแรก **ไม่สร้างใบเสร็จใหม่** — รับประกันว่าใบที่ส่งซ้ำจะตรงกับ
ใบแรกเป๊ะๆ เสมอ

### ฝั่งเจ้าของ — ยืนยันสลิปที่ผู้เช่าส่งเข้ามา

```
ผู้เช่าส่งสลิปเข้า LINE
        │
        ▼
attachSlipToRoom() จับคู่กับบิล + notifyAdmin('slipPending', ...)
        │
        ▼
ข้อความแจ้งเจ้าของ แนบลิงก์ auto-login (role: owner) ไปที่
"/?openSlip=<invoiceId>" (ลิงก์ใช้ได้ 5 นาที)
        │
        ▼
เจ้าของกดลิงก์ → auto-login → componentDidMount อ่าน ?openSlip=
→ เปิด modal "ดูสลิป" ของบิลนั้นให้อัตโนมัติ (หน้าเดิม ไม่ได้สร้างใหม่)
```

## ไฟล์ที่แก้/สร้างใหม่

| ไฟล์ | สิ่งที่ทำ |
|---|---|
| `server/line.js` | เพิ่ม `pushMessageWithConfirmButton()` — ส่ง text/image + ปุ่ม postback ในคำขอเดียว |
| `server/routes/line.js` | เพิ่ม `POST /api/line/invoices/:id/send-with-confirm`, postback handler `action=confirmReceipt`, ขยาย auto-login รองรับ `openSlipInvoiceId`, แนบลิงก์ในข้อความแจ้งสลิปใหม่ |
| `server/routes/scheduler.js` | เพิ่ม block เช็ค 24 ชม. resend/escalate (ไม่มีเงื่อนไข claudeAutomationEnabled — เหมือน overdue-bill check เดิม) |
| `server/coerce.js` | อ่านฟิลด์ใหม่ 4 ตัวจาก Invoices |
| `server/platformVersion.js` | bump เป็น v6 |
| `Rental Management.dc.html` | `sendReceiptLine` เรียก endpoint ใหม่, ปุ่ม "ส่งข้อมูล" ปิดใช้งานจริง, `componentDidMount` เช็ค `?openSlip=` |
| `prototype-auth/migrate-add-receipt-confirm-columns.js` | เพิ่มคอลัมน์ใหม่ 4 ตัวใน Invoices tab (รันแล้วทั้ง 3 ตึกที่รู้จัก) |

## คอลัมน์ใหม่ใน Invoices tab

- `receiptDeliveryConfirmed` — ผู้เช่ากดยืนยันหรือยัง (bool)
- `receiptSendCount` — ส่งไปกี่ครั้งแล้ว (1 = ครั้งแรก, 2 = ส่งซ้ำอัตโนมัติ — สูงสุด 2)
- `receiptLastSentAt` — เวลาส่งล่าสุด (ISO timestamp)
- `receiptOwnerNotified` — แจ้งเจ้าของไปแล้วหรือยัง (กันแจ้งซ้ำทุกรอบ scheduler)

## Staged-rollout gate (v6)

**ไม่เหมือน v4 (ตัว scheduler-reliability fix) ที่ยกเว้นบัญชีหลัก — v6
นี้ไม่ยกเว้นใครเลย รวมบัญชีหลักด้วย** เพราะเป็นพฤติกรรมใหม่จริง ไม่ใช่
การแก้บั๊กคืนพฤติกรรมเดิม

จนกว่าจะกด "🆕 อัปเดต" ให้ตึกไหน:
- ปุ่ม "ส่งข้อมูล (LINE)" ยังกดซ้ำได้เหมือนเดิมทุกประการ (ไม่ปิดใช้งาน)
- ไม่มีปุ่มยืนยันแนบไปกับใบเสร็จ
- scheduler ไม่ resend/escalate อะไรเลยสำหรับตึกนั้น

เช็คจาก 2 จุด: `cfEnforceReceiptConfirm()` (frontend) และ
`RECEIPT_CONFIRM_VERSION` ใน `scheduler.js` (backend, ผ่าน parameter
`platformVersion` ที่ threaded มาจาก `GET /run`'s Directory lookup)

## ทดสอบก่อนเปิดใช้จริง (แนะนำ)

1. กด "🆕 อัปเดต" ให้ตึกทดสอบ (ไม่ใช่ตึกที่มีผู้เช่าจริงใช้งานอยู่ก่อน ถ้าเป็นไปได้)
2. ออกบิล + ส่งใบเสร็จห้องทดสอบ 1 ห้อง — เช็คว่า:
   - ปุ่ม "ส่งข้อมูล" ปิดใช้งานจริงไหม
   - ข้อความ LINE มีปุ่ม "✅ ยืนยันได้รับแล้ว" แนบมาไหม
   - กดยืนยันแล้วสถานะเปลี่ยนเป็น "✓ ยืนยันแล้ว" ไหม
3. ส่งสลิปทดสอบเข้าไป — เช็คว่าข้อความแจ้งเจ้าของมีลิงก์ กดแล้วเปิด modal ดูสลิปถูกบิลไหม
4. รอ/จำลอง 24 ชม. เช็คว่า resend ทำงานถูกต้อง (ใช้ใบเสร็จเดิม ไม่ใช่สร้างใหม่)

---
*บันทึกไว้ 2 ส.ค. 2569 — โค้ดพร้อมใช้งาน รอกด "🆕 อัปเดต" ทดสอบจริงก่อนเปิดให้ทุกตึก*
